# Para concatenar, utilice el script de perl adjunto de la siguiente manera (dentro de la carpeta aligned):

concat=/ruta/directorio/concat

./catfasta2phyml.pl -c -f *al.fasta > $concat/gentooConcat.fasta 2> $concat/partitions.txt
